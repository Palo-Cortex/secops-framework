# H1 + H2: Proofpoint only — email initial access + click
# Tests: Email Analysis, no endpoint correlation
scenario: Turla Carbon — Email Only (H1/H2)
compress_window: 30m

sources:
  - name: ProofPoint TAP
    file: input_tsv/ProofPoint-MITRE-Turla-Carbon-in-XSIAM.tsv
    env: .env-brumxdr-proofpoint
