# Dedup crash-check — Endpoint (Microsoft Defender)
# Confirms endpoint dedup no longer crashes after dedup_match json->text.
# Expect: DBotFindSimilarAlerts runs with similarTextField="agentid,hostname",
#         similarJsonField="", and NO JSONDecodeError.
scenario: Dedup Crash Check - Endpoint (Defender)
compress_window: 5m
sources:
  - name: Microsoft Defender
    file: input_tsv/gunter-2evt-defender.tsv
    env: .env-deathstar-defender
