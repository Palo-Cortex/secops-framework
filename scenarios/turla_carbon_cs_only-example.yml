# Turla Carbon — CrowdStrike only
# Use this when demoing endpoint detection without the email initial access layer.

scenario: Turla Carbon — CrowdStrike Endpoint
compress_window: 2h

sources:
  - name: CrowdStrike
    file: input_tsv/CrowdStrike-MITRE-Turla-Carbon-in-XSIAM.tsv
    env: .env-httpcollector-crowdstrike
