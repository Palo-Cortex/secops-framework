## [1.2.0] - March 2026

### Fixed

#### SOC Microsoft Graph Defender EndPoint — Correlation rule fixes

- Removed `rule_id: 0` — caused SDK to resolve `id` as null at validate time
- Fixed alert name MITRE technique field — replaced raw JSON array render with
  `arrayindex(mitreTechniques -> [], 0)` to extract primary technique as clean string
- Standardised alert name to `[Endpoint] {hostname} | {category} | {technique}`

#### MSGraphMDE_ModelingRule — Modeling rule fixes

- Removed `XDM_CONST.*` enum assignments — not supported in pack-uploaded rules (error 101704)
- Removed `arrayfilter`/`arrayindex` on `evidence` — not valid XQLm; belongs in correlation rule
- Removed invalid XDM fields that do not exist in the schema
- Split from combined two-MODEL file — one MODEL section per file required by SDK
- Schema: added `is_array` to all fields; corrected `json_array` type; trimmed to used fields only

## [1.0.24] - Initial Release

Initial pack release.
